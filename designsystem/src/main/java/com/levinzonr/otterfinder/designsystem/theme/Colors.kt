package com.levinzonr.otterfinder.designsystem.theme

import androidx.compose.ui.graphics.Color

// Primary color palette - warm peachy-beige theme
val Primary = Color(0xFFFFD6A0) // #FFD6A0 - warm peachy-beige
val PrimaryDark = Color(0xFFE6C28F) // Darker shade for dark mode
val PrimaryLight = Color(0xFFFFE8D1) // Lighter shade for accents

// On-primary colors (text/icons on primary background)
val OnPrimary = Color(0xFF5C3A1F) // Dark brown for contrast
val OnPrimaryLight = Color(0xFF8B5A2F) // Slightly lighter for dark mode

// Secondary colors - complementary warm tones
val Secondary = Color(0xFFD4A574) // Warm tan
val SecondaryDark = Color(0xFFB8905F) // Darker tan
val OnSecondary = Color(0xFF2A1A0F) // Dark brown

// Neutral colors
val DarkGrey = Color(0xFF2D2D2D) // Dark grey for backgrounds
val MediumGrey = Color(0xFF6B6B6B) // Medium grey
val LightGrey = Color(0xFFF5F0E8) // Warm off-white/light beige
val Black = Color.Black
val White = Color.White

// Semantic colors
val Error = Color(0xFFC85A3A) // Warm red-orange
val OnError = Color.White
val Success = Color(0xFF8B9A5B) // Muted green
val Warning = Color(0xFFD4A574) // Warm amber
